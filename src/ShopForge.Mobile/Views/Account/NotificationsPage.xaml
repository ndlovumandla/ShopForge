<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopForge.Mobile.Views.Account.NotificationsPage"
             Title="Notifications">
    <Grid RowDefinitions="Auto,*">
        <Button Grid.Row="0" Text="Mark All Read" Command="{Binding MarkAllReadCommand}"
                BackgroundColor="Transparent" TextColor="{StaticResource Primary}"
                HorizontalOptions="End" Margin="16,8" />
        <CollectionView Grid.Row="1" ItemsSource="{Binding Notifications}">
            <CollectionView.EmptyView>
                <Label Text="No notifications" HorizontalOptions="Center" Margin="40" TextColor="{StaticResource Gray600}" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Delete" BackgroundColor="{StaticResource Error}"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}"
                                           CommandParameter="{Binding .}" />
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Frame Margin="16,4" Padding="12" CornerRadius="8" HasShadow="False"
                               BackgroundColor="{Binding IsRead, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=White}">
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                    <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="14" />
                                    <Label Text="{Binding Message}" TextColor="{StaticResource Gray600}" FontSize="13" />
                                    <Label Text="{Binding CreatedAt, StringFormat='{0:MMM dd, HH:mm}'}" TextColor="{StaticResource Gray400}" FontSize="11" />
                                </VerticalStackLayout>
                                <Button Grid.Column="1" Text="&#x2713;" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.MarkReadCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="Transparent" TextColor="{StaticResource Primary}"
                                        IsVisible="{Binding IsRead, Converter={StaticResource InvertedBoolConverter}}" />
                            </Grid>
                        </Frame>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>